[project]
name = "projeto-webscraping"
version = "0.1.0"
description = ""
authors = [
    {name = "Joao Enes"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "scrapy (>=2.13.3,<3.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
